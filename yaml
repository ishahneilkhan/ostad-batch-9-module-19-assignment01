
---

## 🚀 Features

- View list of tasks
- Add new tasks
- Update existing tasks
- Delete tasks
- API integration using HTTP package

---

## 🛠 How to Run

1. **Clone the repository**
    ```
    git clone https://your-repo-link.git
    ```

2. **Navigate to the project folder**
    ```
    cd task_manager_app
    ```

3. **Get the dependencies**
    ```
    flutter pub get
    ```

4. **Run the app**
    ```
    flutter run
    ```

---

## 🔌 Dependencies

Make sure to add these in your `pubspec.yaml`:

```yaml
dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
