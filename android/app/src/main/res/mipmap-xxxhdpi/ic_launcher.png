import 'package:flutter/material.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(
          title: Text('Flutter Logo Example'),
        ),
        body: Center(
          child: FlutterLogo(
            size: 100, // Logo ki size ko yaha customize kar sakte hain
          ),
        ),
      ),
    );
  }
}
